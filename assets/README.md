# Assets Directory

Place any image or icon files here.

Suggested additions:
- `icon.ico`  — application icon (Windows)
- `icon.png`  — application icon (cross-platform)

To apply a window icon in main.py:
```python
root.iconphoto(True, tk.PhotoImage(file="assets/icon.png"))
```
